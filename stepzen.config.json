{
  "create": {
    "commands": [
      "npm install",
      "cp .env.sample .env",
      "stepzen init --yes",
      "stepzen upload schema {{STEPZEN_ENDPOINT}} --dir ./stepzen",
      "stepzen deploy {{STEPZEN_ENDPOINT}} --schema {{STEPZEN_ENDPOINT}}"
    ],
    "instructions": "To start your application:\n\ncd {{WORKING_FOLDER}}\n\nnpm run develop (in one terminal)\n\nstepzen start (in another)"
  }
}